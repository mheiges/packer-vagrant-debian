---
- hosts: all
  become: yes
  gather_facts: no
  remote_user: vagrant

  roles:
    - mheiges.packer-debian

  tasks:
    - apt: 
        name: "{{ item }}"
        state: present
      with_items:
        - git
